%YAML 1.2
---
connections:
  default:
    # to use a real mongo server switch the lines below
    # and start up docker-compose-dev
    uri: mongomock://localhost
    #uri: mongodb://api-server:example@mongo:27017/cyhy
